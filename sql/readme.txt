How to executing the sql file:
log into your database first by command tools, then execute:
source path/to/database.sql

Commands for Daniel's Convenience:
export PATH=$PATH:/usr/local/mysql/bin
mysql -u root -p